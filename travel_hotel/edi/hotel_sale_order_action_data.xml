<?xml version="1.0" ?>
<openerp>
    <data noupdate="0">

        <!--Email template -->
        <record id="hotel_supplier_email_template" model="email.template">
            <field name="name">Sales Order Line- Send by Email</field>
            <field name="email_from">${(object.supplier_id.email or '')|safe}</field>
            <field name="subject">
                About ...
            </field>
            <field name="partner_to">${object.order_id.partner_invoice_id.id or 'n/a'}</field>
            <field name="model_id" ref="sale.model_sale_order_line"/>
            <field name="auto_delete" eval="True"/>
            <!--<field name="report_template" ref="report_sale_order"/>-->
            <field name="report_name">
                ${(object.order_id.name or '').replace('/','_')}_${object.order_id.state == 'draft' and 'draft' or ''}
            </field>
            <field name="lang">${object.order_id.partner_id.lang or 'es'}</field>
            <field name="body_html"><![CDATA[
<div style="font-family: 'Lucica Grande', Ubuntu, Arial, Verdana, sans-serif; font-size: 12px; color: rgb(34, 34, 34); background-color: #FFF; ">

    <p>Hello ${object.supplier_id.name},</p>

    <p>Here is your ${object.order_id.state in ('draft', 'sent') and 'quotation' or 'order confirmation'} from ${object.order_id.company_id.name}: </p>

    <p style="border-left: 1px solid #8e0000; margin-left: 30px;">
       &nbsp;&nbsp;<strong>REFERENCES</strong><br />
       &nbsp;&nbsp;Order number: <strong>${object.order_id.name}</strong><br />
       &nbsp;&nbsp;Order total: <strong>${object.order_id.amount_total} ${object.order_id.pricelist_id.currency_id.name}</strong><br />
       &nbsp;&nbsp;Order date:  ${object.order_id.date_order} <br />
       % if object.origin:
       &nbsp;&nbsp;Order reference: ${object.origin}<br />
       % endif
       % if object.order_id.client_order_ref:
       &nbsp;&nbsp;Your reference: ${object.order_id.client_order_ref}<br />
       % endif
       % if object.order_id.user_id:
       &nbsp;&nbsp;Your contact: <a href="mailto:${object.order_id.user_id.email or ''}?subject=Order%20${object.order_id.name}">${object.order_id.user_id.name}</a>
       % endif
    </p>

    % if object.order_id.paypal_url:
    <br/>
    <p>It is also possible to directly pay with Paypal:</p>
        <a style="margin-left: 120px;" href="${object.order_id.paypal_url}">
            <img class="oe_edi_paypal_button" src="/sale/static/img/btn_paynowcc_lg.gif"/>
        </a>
    % endif

    <br/>
    <p>If you have any question, do not hesitate to contact us.</p>
    <p>Thank you for choosing ${object.order_id.company_id.name or 'us'}!</p>
    <br/>
    <br/>
    <div style="width: 375px; margin: 0px; padding: 0px; background-color: #8E0000; border-top-left-radius: 5px 5px; border-top-right-radius: 5px 5px; background-repeat: repeat no-repeat;">
        <h3 style="margin: 0px; padding: 2px 14px; font-size: 12px; color: #DDD;">
            <strong style="text-transform:uppercase;">${object.order_id.company_id.name}</strong></h3>
    </div>
    <div style="width: 347px; margin: 0px; padding: 5px 14px; line-height: 16px; background-color: #F2F2F2;">
        <span style="color: #222; margin-bottom: 5px; display: block; ">
        % if object.order_id.company_id.street:
            ${object.order_id.company_id.street}<br/>
        % endif
        % if object.order_id.company_id.street2:
            ${object.order_id.company_id.street2}<br/>
        % endif
        % if object.order_id.company_id.city or object.order_id.company_id.zip:
            ${object.order_id.company_id.zip} ${object.order_id.company_id.city}<br/>
        % endif
        % if object.order_id.company_id.country_id:
            ${object.order_id.company_id.state_id and ('%s, ' % object.order_id.company_id.state_id.name) or ''} ${object.order_id.company_id.country_id.name or ''}<br/>
        % endif
        </span>
        % if object.order_id.company_id.phone:
            <div style="margin-top: 0px; margin-right: 0px; margin-bottom: 0px; margin-left: 0px; padding-top: 0px; padding-right: 0px; padding-bottom: 0px; padding-left: 0px; ">
                Phone:&nbsp; ${object.order_id.company_id.phone}
            </div>
        % endif
        % if object.order_id.company_id.website:
            <div>
                Web :&nbsp;<a href="${object.order_id.company_id.website}">${object.order_id.company_id.website}</a>
            </div>
        %endif
        <p></p>
    </div>
</div>
            ]]></field>
        </record>
    </data>
</openerp>
